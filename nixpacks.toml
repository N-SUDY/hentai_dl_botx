[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "ffmpeg"]
aptPkgs = ["ffmpeg"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt", "chmod +x binary/N_m3u8DL-RE"]

[start]
cmd = "python3 app.py"
